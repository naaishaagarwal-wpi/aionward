import type { Testimonial } from "@/lib/types/content";

/**
 * PLACEHOLDER — do not invent testimonials or participant names.
 * Add real quotes only when participants have shared them for use on the site.
 */
export const testimonials: Testimonial[] = [];
