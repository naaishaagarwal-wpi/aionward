import type { Workshop } from "@/lib/types/content";

/**
 * PLACEHOLDER — do not invent workshop details.
 * Add real workshops here when confirmed (title, date, audience, location, photos, etc.).
 * Shape matches future Contentful Workshop content type.
 */
export const workshops: Workshop[] = [];
